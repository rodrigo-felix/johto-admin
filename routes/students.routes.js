const { Router } = require('express');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');

const Students = require('../models/Students.model');

const router = Router();

// get all students

// get specifc student

// post new student

// edit specifc student

// delete specific student

module.exports = router;